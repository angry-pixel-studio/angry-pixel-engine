name: Reusable Workflow Build Code

on:
    workflow_call:

jobs:
    build:
        name: "[Build Core]"
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: "[Run build]"
              run: yarn run build
